{
  "config": {
    "step": {
      "user": {
        "title": "❱ Plex Assistant Setup",
        "description": "[Documentation](https://github.com/JohanOstlund/plex_assistant)",
        "data": {
          "server_name": "Select Plex server",
          "language": "Language",
          "default_cast": "Default cast device",
          "tts_errors": "Speak errors on cast device"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single instance is allowed.",
      "no_plex_server": "No Plex servers were found, be sure that the Home Assistant Plex integration is setup and working."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "❱ Plex Assistant Options",
        "description": "Source priority is a comma separated list, e.g. `plex, netflix, max`. Titles are played from the first source in the list that has them; saying \"on netflix\"/\"på netflix\" in a command always overrides the priority.",
        "data": {
          "source_priority": "Source priority (comma separated, first wins)",
          "region": "Streaming region (ISO country code)",
          "plex_token": "Plex.tv account token (leave empty to reuse the Plex integration's token)",
          "start_script": "Run a script to start an unavailable Plex client",
          "keyword_replace": "Replace keywords or phrases with your own",
          "jump_f": "Default jump forward amount in seconds",
          "jump_b": "Default jump backward amount in seconds"
        }
      }
    }
  }
}
